[package]
name = "rusterix-core"
version = "0.1.0"
edition = "2024"
description = "Core runtime library for ASTERIX message encoding and decoding"

[dependencies]

[dev-dependencies]
test-utils = { path = "../test-utils" }
