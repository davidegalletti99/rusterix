# ! [allow (unused_imports)] # ! [allow (dead_code)] use rusterix :: framework :: { BitReader , BitWriter , DecodeError , Fspec , Decode , Encode } ; use std :: io :: { Read , Write } ; # [doc = r" ASTERIX Category record."] # [doc = r" "] # [doc = r" Contains optional data items, each controlled by a bit in the FSPEC."] # [derive (Debug , Clone , PartialEq)] pub struct Cat048Record { pub item020 : Option < Item020 > } impl Cat048Record { # [doc = r" Decodes a record from a binary stream."] # [doc = r" "] # [doc = r" Reads the FSPEC to determine which items are present, then"] # [doc = r" decodes only the present items."] # [doc = r" "] # [doc = r" # Arguments"] # [doc = r" "] # [doc = r" * `reader` - The input stream to read from"] # [doc = r" "] # [doc = r" # Errors"] # [doc = r" "] # [doc = r" Returns an error if reading or parsing fails."] pub fn decode < R : std :: io :: Read > (reader : & mut R) -> Result < Self , DecodeError > { let fspec = Fspec :: read (reader) ? ; let mut bit_reader = BitReader :: new (reader) ; Ok (Self { item020 : if fspec . is_set (0usize , 7u8) { Some (Item020 :: decode (& mut bit_reader) ?) } else { None } }) } } impl Cat048Record { # [doc = r" Encodes a record to a binary stream."] # [doc = r" "] # [doc = r" Automatically constructs the FSPEC based on which items are present,"] # [doc = r" then encodes all present items."] # [doc = r" "] # [doc = r" # Arguments"] # [doc = r" "] # [doc = r" * `writer` - The output stream to write to"] # [doc = r" "] # [doc = r" # Errors"] # [doc = r" "] # [doc = r" Returns an error if writing fails."] pub fn encode < W : std :: io :: Write > (& self , writer : & mut W) -> Result < () , DecodeError > { let mut fspec = Fspec :: new () ; if self . item020 . is_some () { fspec . set (0usize , 7u8) ; } fspec . write (writer) ? ; let mut bit_writer = BitWriter :: new (writer) ; if let Some (ref item) = self . item020 { item . encode (& mut bit_writer) ? ; } bit_writer . flush () ? ; Ok (()) } } # [derive (Debug , Clone , PartialEq)] pub struct Item020Part0 { pub a : u8 , pub b : u8 } # [derive (Debug , Clone , PartialEq)] pub struct Item020Part1 { pub c : u8 } # [derive (Debug , Clone , PartialEq)] pub struct Item020 { pub part0 : Item020Part0 , pub part1 : Option < Item020Part1 > } impl Item020Part0 { pub fn decode < R : std :: io :: Read > (reader : & mut BitReader < R > ,) -> Result < Self , DecodeError > { let a = reader . read_bits (3usize) ? as u8 ; let b = reader . read_bits (4usize) ? as u8 ; Ok (Self { a , b }) } } impl Item020Part1 { pub fn decode < R : std :: io :: Read > (reader : & mut BitReader < R > ,) -> Result < Self , DecodeError > { let c = reader . read_bits (5usize) ? as u8 ; reader . read_bits (2usize) ? ; Ok (Self { c }) } } impl Decode for Item020 { fn decode < R : std :: io :: Read > (reader : & mut BitReader < R > ,) -> Result < Self , DecodeError > { let part0 = Item020Part0 :: decode (reader) ? ; let mut fx = reader . read_bits (1) ? != 0 ; let part1 = if fx { let part = Item020Part1 :: decode (reader) ? ; fx = reader . read_bits (1) ? != 0 ; Some (part) } else { None } ; Ok (Self { part0 , part1 }) } } impl Item020Part0 { pub fn encode < W : std :: io :: Write > (& self , writer : & mut BitWriter < W > ,) -> Result < () , DecodeError > { writer . write_bits (self . a as u64 , 3usize) ? ; writer . write_bits (self . b as u64 , 4usize) ? ; Ok (()) } } impl Item020Part1 { pub fn encode < W : std :: io :: Write > (& self , writer : & mut BitWriter < W > ,) -> Result < () , DecodeError > { writer . write_bits (self . c as u64 , 5usize) ? ; writer . write_bits (0 , 2usize) ? ; Ok (()) } } impl Encode for Item020 { fn encode < W : std :: io :: Write > (& self , writer : & mut BitWriter < W > ,) -> Result < () , DecodeError > { self . part0 . encode (writer) ? ; writer . write_bits (self . part1 . is_some () as u64 , 1) ? ; if let Some (ref part_data) = self . part1 { part_data . encode (writer) ? ; writer . write_bits (0 , 1) ? ; } Ok (()) } }