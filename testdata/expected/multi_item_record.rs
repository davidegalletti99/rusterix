# ! [allow (unused_imports)] # ! [allow (dead_code)] use rusterix :: rcore :: { BitReader , BitWriter , DecodeError , Fspec , Decode , Encode } ; use std :: io :: { Read , Write } ; # [doc = r" ASTERIX Category record."] # [doc = r" "] # [doc = r" Contains optional data items, each controlled by a bit in the FSPEC."] # [derive (Debug , Clone , PartialEq)] pub struct Cat048Record { pub item010 : Option < Item010 > , pub item020 : Option < Item020 > } impl Cat048Record { # [doc = r" Decodes a record from a binary stream."] # [doc = r" "] # [doc = r" Reads the FSPEC to determine which items are present, then"] # [doc = r" decodes only the present items."] # [doc = r" "] # [doc = r" # Arguments"] # [doc = r" "] # [doc = r" * `reader` - The input stream to read from"] # [doc = r" "] # [doc = r" # Errors"] # [doc = r" "] # [doc = r" Returns an error if reading or parsing fails."] pub fn decode < R : std :: io :: Read > (reader : & mut R) -> Result < Self , DecodeError > { let fspec = Fspec :: read (reader) ? ; let mut bit_reader = BitReader :: new (reader) ; Ok (Self { item010 : if fspec . is_set (0usize , 0u8) { Some (Item010 :: decode (& mut bit_reader) ?) } else { None } , item020 : if fspec . is_set (0usize , 1u8) { Some (Item020 :: decode (& mut bit_reader) ?) } else { None } }) } } impl Cat048Record { # [doc = r" Encodes a record to a binary stream."] # [doc = r" "] # [doc = r" Automatically constructs the FSPEC based on which items are present,"] # [doc = r" then encodes all present items."] # [doc = r" "] # [doc = r" # Arguments"] # [doc = r" "] # [doc = r" * `writer` - The output stream to write to"] # [doc = r" "] # [doc = r" # Errors"] # [doc = r" "] # [doc = r" Returns an error if writing fails."] pub fn encode < W : std :: io :: Write > (& self , writer : & mut W) -> Result < () , DecodeError > { let mut fspec = Fspec :: new () ; if self . item010 . is_some () { fspec . set (0usize , 0u8) ; } if self . item020 . is_some () { fspec . set (0usize , 1u8) ; } fspec . write (writer) ? ; let mut bit_writer = BitWriter :: new (writer) ; if let Some (ref item) = self . item010 { item . encode (& mut bit_writer) ? ; } if let Some (ref item) = self . item020 { item . encode (& mut bit_writer) ? ; } bit_writer . flush () ? ; Ok (()) } } # [derive (Debug , Clone , PartialEq)] pub struct Item010 { pub sac : u8 , pub sic : u8 } impl Decode for Item010 { fn decode < R : std :: io :: Read > (reader : & mut BitReader < R > ,) -> Result < Self , DecodeError > { let sac = reader . read_bits (8usize) ? as u8 ; let sic = reader . read_bits (8usize) ? as u8 ; Ok (Self { sac , sic }) } } impl Encode for Item010 { fn encode < W : std :: io :: Write > (& self , writer : & mut BitWriter < W > ,) -> Result < () , DecodeError > { writer . write_bits (self . sac as u64 , 8usize) ? ; writer . write_bits (self . sic as u64 , 8usize) ? ; Ok (()) } } # [derive (Debug , Clone , PartialEq)] pub struct Item020 { pub typ : u8 } impl Decode for Item020 { fn decode < R : std :: io :: Read > (reader : & mut BitReader < R > ,) -> Result < Self , DecodeError > { let typ = reader . read_bits (8usize) ? as u8 ; Ok (Self { typ }) } } impl Encode for Item020 { fn encode < W : std :: io :: Write > (& self , writer : & mut BitWriter < W > ,) -> Result < () , DecodeError > { writer . write_bits (self . typ as u64 , 8usize) ? ; Ok (()) } }